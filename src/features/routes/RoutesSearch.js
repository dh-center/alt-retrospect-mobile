import {Layout, Text} from '@ui-kitten/components';
import React from 'react';

export const RoutesSearch = props => {
    return (
        <Layout level="3">
            <Text>Search</Text>
        </Layout>
    );
};
